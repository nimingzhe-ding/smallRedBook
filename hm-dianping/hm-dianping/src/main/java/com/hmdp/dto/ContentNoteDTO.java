package com.hmdp.dto;

/**
 * Backward-compatible alias used by existing feed/search code.
 * New controller contracts should expose NoteDTO / NoteDetailDTO semantics.
 */
public class ContentNoteDTO extends NoteDetailDTO {
}
